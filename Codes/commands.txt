# Step 1: Upgrade pip
python -m pip install --upgrade pip

# Step 2: Install core dependencies
python -m pip install numpy pandas opencv-python Pillow requests

pip install path\to\dlib‑19.24.1‑cp310‑cp310‑win_amd64.whl

Path -> File Folder


# Step 3: Install face recognition packages
python -m pip install face-recognition face-recognition-models

# Step 4: Install supporting packages
python -m pip install click colorama tqdm moviepy

# Step 5: Verify installations
python -c "import pandas, numpy, cv2, PIL, requests, face_recognition, click, colorama, tqdm, moviepy; print('All libraries installed successfully!')"

# Step 6:
pip install -r requirements.txt
